"use client";

import { CustomLayout } from "@/components/customeLayout";
import { useState } from "react";
import { 
  Search, 
  GraduationCap, 
  ClipboardCheck, 
  AlertCircle, 
  Download,
  Printer,
  CheckCircle2
} from "lucide-react";
import {
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  DialogDescription,
} from "@/components/ui/dialog";

export default function Result() {
  const [loading, setLoading] = useState(false);
  const [showDemo, setShowDemo] = useState(false);

  const handleCheckResult = (e: React.FormEvent) => {
    e.preventDefault();
    setLoading(true);
    
    // Simulating a backend delay
    setTimeout(() => {
      setLoading(false);
      setShowDemo(true);
    }, 1500);
  };

  return (
    <CustomLayout>
      <div className="max-w-7xl mx-auto py-12 md:py-20 px-4 md:px-16 min-h-[80vh] flex flex-col items-center">
        
        {/* Header Section */}
        <div className="text-center mb-12">
          <div className="inline-flex items-center justify-center p-3 bg-blue-50 rounded-2xl text-blue-600 mb-4">
            <ClipboardCheck size={32} />
          </div>
          <h1 className="text-3xl md:text-5xl font-bold text-blue-900 mb-4 tracking-tight">Examination Results</h1>
          <p className="text-slate-500 max-w-lg mx-auto text-sm md:text-base">
            Enter your credentials below to access your digital mark sheet for the 
            Academic Session 2025-26.
          </p>
        </div>

        {/* Result Form Card */}
        <div className="w-full max-w-md bg-white border border-slate-100 p-8 rounded-[2.5rem] shadow-xl shadow-blue-900/5">
          <form onSubmit={handleCheckResult} className="space-y-5">
            
            <div className="space-y-2">
              <label className="text-xs font-bold text-slate-400 uppercase tracking-widest ml-1">Registration Number</label>
              <input 
                required
                type="text" 
                placeholder="e.g. REG-2026-XXXX"
                className="w-full px-5 py-4 bg-slate-50 border border-slate-100 rounded-2xl focus:outline-none focus:ring-2 focus:ring-blue-600 focus:bg-white transition-all text-sm font-medium"
              />
            </div>

            <div className="space-y-2">
              <label className="text-xs font-bold text-slate-400 uppercase tracking-widest ml-1">Roll Number</label>
              <input 
                required
                type="number" 
                placeholder="Enter Examination Roll No."
                className="w-full px-5 py-4 bg-slate-50 border border-slate-100 rounded-2xl focus:outline-none focus:ring-2 focus:ring-blue-600 focus:bg-white transition-all text-sm font-medium"
              />
            </div>

            <div className="space-y-2">
              <label className="text-xs font-bold text-slate-400 uppercase tracking-widest ml-1">Course / Semester</label>
              <select className="w-full px-5 py-4 bg-slate-50 border border-slate-100 rounded-2xl focus:outline-none focus:ring-2 focus:ring-blue-600 focus:bg-white transition-all text-sm font-medium appearance-none">
                <option>B.A. Semester II</option>
                <option>B.Sc. Semester II</option>
                <option>B.Com. Semester II</option>
                <option>Vocational BCA Part I</option>
              </select>
            </div>

            <button 
              disabled={loading}
              className="w-full bg-blue-900 hover:bg-blue-800 text-white font-bold py-4 rounded-2xl shadow-lg shadow-blue-900/20 transition-all flex items-center justify-center gap-2 active:scale-[0.98] disabled:opacity-70"
            >
              {loading ? (
                <div className="h-5 w-5 border-2 border-white/30 border-t-white rounded-full animate-spin" />
              ) : (
                <>
                  <Search size={18} />
                  Fetch Result
                </>
              )}
            </button>
          </form>

          <div className="mt-8 p-4 bg-amber-50 rounded-2xl flex gap-3 items-start border border-amber-100">
            <AlertCircle size={18} className="text-amber-600 shrink-0 mt-0.5" />
            <p className="text-[11px] text-amber-700 leading-relaxed">
              If you face issues with your registration ID, please contact the 
              Examination Controller's office immediately.
            </p>
          </div>
        </div>

        {/* --- DEMO RESULT POPUP --- */}
        <Dialog open={showDemo} onOpenChange={setShowDemo}>
          <DialogContent className="max-w-2xl rounded-[2rem] p-0 overflow-hidden border-none shadow-2xl">
            <div className="bg-blue-900 p-8 text-white text-center">
              <CheckCircle2 className="mx-auto mb-4 text-emerald-400" size={48} />
              <DialogTitle className="text-2xl font-bold">Result Found!</DialogTitle>
              <DialogDescription className="text-blue-200 mt-1">
                Statement of Marks - Provisional
              </DialogDescription>
            </div>

            <div className="p-8 space-y-6">
              {/* Student Bio Info */}
              <div className="grid grid-cols-2 gap-4 pb-6 border-b border-slate-100">
                <div>
                  <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Student Name</p>
                  <p className="text-sm font-bold text-slate-800">DEMO STUDENT NAME</p>
                </div>
                <div className="text-right">
                  <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Result Status</p>
                  <p className="text-sm font-bold text-emerald-600">PASSED (FIRST DIV)</p>
                </div>
              </div>

              {/* Demo Marks Table */}
              <div className="space-y-3">
                {[
                  { sub: "Major Course 1", marks: "78/100" },
                  { sub: "Minor Course 1", marks: "82/100" },
                  { sub: "MDC / Elective", marks: "65/100" },
                  { sub: "SEC / VAC", marks: "42/50" },
                ].map((item, i) => (
                  <div key={i} className="flex justify-between items-center p-3 bg-slate-50 rounded-xl">
                    <span className="text-xs font-semibold text-slate-600">{item.sub}</span>
                    <span className="text-sm font-bold text-slate-800">{item.marks}</span>
                  </div>
                ))}
              </div>

              {/* Total Row */}
              <div className="flex justify-between items-center p-5 bg-blue-50 rounded-2xl border border-blue-100">
                <span className="font-bold text-blue-900">Aggregate Percentage</span>
                <span className="text-2xl font-black text-blue-900">76.8%</span>
              </div>

              {/* Actions */}
              <div className="flex gap-3 pt-4">
                <button className="flex-1 flex items-center justify-center gap-2 bg-slate-900 text-white py-3 rounded-xl text-sm font-bold hover:bg-slate-800 transition-all">
                  <Download size={16} /> Download PDF
                </button>
                <button className="flex items-center justify-center w-12 h-12 bg-slate-100 text-slate-600 rounded-xl hover:bg-slate-200 transition-all">
                  <Printer size={18} />
                </button>
              </div>
            </div>
          </DialogContent>
        </Dialog>

      </div>
    </CustomLayout>
  );
}